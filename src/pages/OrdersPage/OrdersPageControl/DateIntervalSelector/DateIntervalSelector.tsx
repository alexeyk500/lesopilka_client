import React from 'react';
import classes from './DateIntervalSelector.module.css';

const DateIntervalSelector = () => {
  return <div className={classes.container}>DateIntervalSelector</div>;
};

export default DateIntervalSelector;
