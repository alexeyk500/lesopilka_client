import React from 'react';
import classes from './MenuContent.module.css';

const MenuContent: React.FC = () => {
  return <div className={classes.container}>MenuContent</div>;
};

export default MenuContent;
