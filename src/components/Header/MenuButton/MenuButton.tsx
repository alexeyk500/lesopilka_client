import React from 'react';
import classes from '../LoginButton/LoginButton.module.css';

const MenuButton: React.FC = () => {
  return <div className={classes.container}>Меню</div>;
};

export default MenuButton;
